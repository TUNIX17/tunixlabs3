'use client';

/**
 * TerminalClient — placeholder island for the prompt/typing engine.
 *
 * Sprint 2 (ssr-split): returns null. Sprint 3 will move the terminal
 * content (prompt typing, command swap, cursor) from v3/page.tsx into
 * this island so it can be loaded with `ssr:false` without touching
 * the server HTML contract.
 */
export default function TerminalClient() {
  return null;
}
