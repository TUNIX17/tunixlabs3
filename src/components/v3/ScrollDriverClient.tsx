'use client';

/**
 * ScrollDriverClient — placeholder island for scroll/rAF driver.
 *
 * Sprint 2 (ssr-split): returns null. Sprint 3 will host the
 * `useActiveSection` + scroll velocity rAF loop here so RSC pages never
 * reference `window` during render.
 */
export default function ScrollDriverClient() {
  return null;
}
