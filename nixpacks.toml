# Configuración de Nixpacks para Next.js
# Forzar Node.js e ignorar detección de Python

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

# Forzar provider de Node
[variables]
NIXPACKS_NO_CACHE = "1"
